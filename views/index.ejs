<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>🐼 Tipsy Panda</title>
    <link rel="stylesheet" href="/main.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>
    <header>
        <h1>🐼 Tipsy Panda</h1>
    </header>
    <div class="container">
        <div class="column shadow-lg p-3 mb-5 bg-body-tertiary rounded">
            <h2><%= drink.strDrink || "No drink selected" %></h2>
            <ul>
                <% ingredients.forEach(item => { %>
                    <li><%= item.ingredient %> - <%= item.measure %></li>
                <% }) %>
            </ul>
            <p><strong>Instructions:</strong> <%= drink.strInstructions || "N/A" %></p>
            <p><strong>Glass:</strong> <%= drink.strGlass || "N/A" %></p>
            <button class="btn btn-outline-secondary" onclick="window.location.href='/'">Click for random cocktail</button>
        </div>
        <div class="column shadow-lg p-3 mb-5 bg-body-tertiary rounded">
            <h2><%= drink.strDrink || "No drink selected" %></h2>
            <img src="<%= drink.strDrinkThumb || '' %>" alt="<%= drink.strDrink || 'No image available' %>">
        </div>
        <div class="column shadow-lg p-3 mb-5 bg-body-tertiary rounded">
            <form action="/search" method="POST">
              <div class="input-group mb-3">
                <input type="text" name="id" class="form-control" placeholder="Search for a cocktail">
                <button class="btn btn-outline-secondary" type="submit">Go!</button>
              </div>
            </form>
            <div class="results">
                <%- content %>
            </div>
        </div>
    </div>
    <footer>
        <p>© 2024 Tipsy Panda</p>
    </footer>
</body>
</html>